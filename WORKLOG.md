# 作業ログ

株情報 Slack ボットの機能追加・コスト最適化の記録。

---

## 2026-06-03 セッション

### サマリ
日次レポートに「注視アカウント」「材料ダイジェスト」「有望株レーダー」の3機能を追加し、
同時に xAI API（Live Search）の課金を最適化した。Slack 銘柄ウォッチャー一式も版管理下に追加。

### 全体アーキテクチャ（日次レポート: `scheduler.py job()`）

| 順 | セクション | モジュール | xAI Live Search | データ源 |
|---|---|---|---|---|
| 1 | 🐦 X注目銘柄 | `fetcher` | あり | X |
| 2 | 📊 値上がりランキング | `scrapers/aggregator` | なし | みんかぶ/Yahoo/株探(スクレイピング) |
| 3 | 📰 材料ダイジェスト | `news_digest` | **なし** | 株探ニュース(スクレイピング)+LLM |
| 4 | 🔎 有望株レーダー | `opportunity_radar` | **なし** | TrendForce+株探+LLM |
| 5 | 💴 100万円戦略 | `strategy` | **なし(除去済)** | LLM |
| 6 | 🌡️ バブル恐怖指数(週1) | `bubble` | あり | X+Web |
| 7 | 👀 注視アカウント | `accounts` | あり | X |
| 8 | 🔍 ウォッチリスト個別分析 | `analyzer` | あり | X |

---

### 追加機能

#### 1. 注視アカウント (`src/accounts.py`)
- `@pelositracker`（米議員の株取引開示）と `@realDonaldTrump`（市場に影響する発言）の
  直近投稿から**株式関連の内容だけ**を抽出して要約。
- 対象は `WATCH_ACCOUNTS` リストで管理（増減容易）。
- Slack 表示位置はバブル恐怖指数の後。

#### 2. 材料ダイジェスト (`src/news_digest.py` + `src/scrapers/kabutan_news.py`)
- ウォッチリスト＋値上がりランキング上位の日本株（最大8銘柄）の株探ニュースを
  **順次スクレイピング**し、**1回のLLM呼び出し**で材料ダイジェスト化。
- LLMは「取得」ではなく「要約・材料判定」に使用（Live Search不要＝安価）。
- 定型特集等のノイズを除外し、決算・業績修正・受注等の個別材料を優先。

#### 3. 有望株レーダー (`src/opportunity_radar.py` + `src/scrapers/trendforce.py`)
**狙い**: 「上流の実世界シグナル → 受益銘柄 → まだ織り込まれていないか → 反復で確信度蓄積」。
キオクシアのように、メモリ価格高騰などの上流シグナルを株価反映前に捉える発想。

フロー:
1. 上流シグナル収集: TrendForce（メモリDRAM/NAND・HBM・半導体の業界動向）見出し
2. LLM-A: 盛り上がりつつあるテーマを抽出（因果仮説＋signal強度1-5＋候補コード）
3. 裏付け: 候補の日本株を株探個別ページで PER/PBR/株価/年初来高安 付与
4. LLM-B: 出遅れ度・指標・懐疑を加味してスコアリング＆ランク（強気仮説/リスク/エントリー）

**反復の仕組み**:
- **L1 時系列** (`radar_state.json`): テーマ別signalを保存し、翌日以降「🆕新規 / 📈強化中」を検出。
- **L2 フィードバック** (`picks_log.json`): 拾った銘柄を価格付きで記録し、`MATURE_DAYS=7`日後の
  先行リターンを自動評価。平均リターン/勝率をLLM-Bに還元して効いた型を優先（自己改善ループ）。

調整: `MATURE_DAYS`（評価日数）, `MAX_ENRICH`（裏付け銘柄上限=10）, `MAX_EVAL_PER_RUN`（=5）。

---

### コスト最適化

| 施策 | 内容 | 効果(概算) |
|---|---|---|
| 10万円戦略を廃止 | `scheduler` から削除 | −$8〜12/月 |
| バブル評価を週1化 | 月曜JSTのみ（`BUBBLE_WEEKDAY=0`）。`--now`では強制実行 | −$13〜20/月 |
| 100万戦略のLive Search除去 | データは既にプロンプトへ渡済→`tools`削除で純トークン化 | −$8〜11/月 |
| 新機能はLive Search不使用 | 材料ダイジェスト/有望株レーダーは全てトークンのみ | +$3〜4/月のみ |

**月額の推移**: 開始時 約\$55〜85 → 現在 約\$42〜54（≒¥6,500〜8,000）。

**コスト構造の要点**: 課金の大半は Live Search（\$0.025/ソース）。トークン代は全LLM合計でも月約\$4。
→ スクレイピング＋LLM要約の構成は Live Search を使わないため安い。
残る課金検索は「Xからしか取れないコア機能」（X注目銘柄/注視アカウント/ウォッチリスト分析/週1バブル）のみ。

---

### 検討したが見送った事項
- **バブル指標のスクレイピング化**: 週1化で削減効果が約\$2.6/月まで低下し、対象サイトの多くが
  JSレンダリングで脆く保守負担大。ROI逆転のため**見送り**。
- **Yahoo株掲示板スクレイピング**: 投稿本文が初期DOMに無く（JS/内部API依存）、ToS的にもグレー、
  ノイズ多。実測でも取得不可のため**不採用**。日本株センチメントはX Searchを継続使用。

### スクレイピング実測メモ（堅牢性）
- ✅ 株探: 指標(`table` th/td)・ニュース(`table.s_news_list`)・テーマ(`?theme=`)・個別ページ
- ✅ TrendForce: `a[href*='/news/']`（タグ無し40字未満はナビとして除外）
- ✅ 価格.com SSDランキング: 価格高騰の解説文が取得可（消費者サイドの裏付け候補・未実装）
- ❌ 日経公式PER/PBR・Yahoo日経VI・松井信用評価損益率・Yahoo掲示板: JSレンダリングで不可

---

### Slack 銘柄ウォッチャー（版管理下に追加）
既存の常駐ツール一式をコミット。Slackチャンネルを監視→投稿から銘柄コード検出→
3ソース集約→Grok買い判定→スレッド返信。Windowsタスクスケジューラ用スクリプトも追加。
※ デプロイスクリプトは実行環境固有の絶対パスをハードコード（別マシンでは要修正）。

---

### コミット
- `c51d7fb` feat: 注目銘柄収集・有望株レーダー用モジュール
- `97de73d` feat: 日次レポート統合＋API課金最適化
- `c83e864` feat: Slack銘柄ウォッチャー機能
- `16cda67` chore: ウォッチャーのWindowsタスク登録・起動スクリプト

### 実行・テスト
```
python scheduler.py --now    # 日次ジョブを即時1回実行（Slackへ投稿。--nowではバブルも強制実行）
python scheduler.py          # 常駐（毎日08:00）
```
本番は GitHub Actions `daily-stock.yml` で毎朝07:30 JST 起動（workflow_dispatch で手動実行も可）。

### 未処理・今後の候補
- `build_cmd.py` は未コミット（使い捨てジェネレータのため保留）。
- usage ロギング（各レスポンスのトークン数・検索ソース数を記録）→ コスト実測化。
- 有望株レーダーの上流シグナル追加（価格.com 消費者価格など）。
- L2フィードバックのテーマ別集計（現状は全体集計）。
